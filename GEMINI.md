# Gemini Project Entry Point

Follow `AGENTS.md` as the standing engineering contract. Then read `docs/VISUAL_SYSTEM.md`, `docs/ARCHITECTURE.md`, and the single prompt the user names.

Execute only one numbered prompt at a time. Begin every session with repository inspection and a concise state/assumptions summary. End with verification evidence, documentation updates, unresolved risks, and the next prompt number. Do not advance past a checkpoint without explicit user approval.

Never modify the separate `yorkstead-website` repository unless the active prompt explicitly scopes a public-site change there. When inspection is allowed, treat it as read-only reference material.
